import {AppRegistry} from 'react-native';
import SEAPHAGES_App_RN from './app/index.js';

//register the main app component; only needs to happen once per app.
AppRegistry.registerComponent('SEAPHAGES_App_RN', () => SEAPHAGES_App_RN);